info:
  name: Top Courses
  type: http
  seq: 2

http:
  method: POST
  url: "{{support2_base}}/stud/topMenus/courses?unitid={{unitId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/tm_course_schedule."
