info:
  name: Top Schedules
  type: http
  seq: 3

http:
  method: POST
  url: "{{support2_base}}/stud/topMenus/TopSchedules?empid={{empId}}&roomid={{roomId}}&courseid={{courseId}}&unitid={{unitId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/tm_course_schedule."
