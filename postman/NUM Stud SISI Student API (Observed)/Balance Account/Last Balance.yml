info:
  name: Last Balance
  type: http
  seq: 4

http:
  method: POST
  url: "{{support2_base}}/stud/BalanceAccount/lastUldegdel?sih={{sih}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/payinfo and #/studentcard."
