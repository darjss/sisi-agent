info:
  name: Previous Balance
  type: http
  seq: 3

http:
  method: POST
  url: "{{support2_base}}/stud/BalanceAccount/befUldegdel?sih={{sih}}&year={{year}}&sem={{sem}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/payinfo."
