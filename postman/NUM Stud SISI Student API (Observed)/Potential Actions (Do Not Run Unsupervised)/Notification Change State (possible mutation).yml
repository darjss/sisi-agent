info:
  name: Notification Change State (possible mutation)
  type: http
  seq: 1

http:
  method: GET
  url: "{{tree_base}}/comnity/Notification/ChangeState?session={{session}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Potential state-changing endpoint from JS bundle string extraction.
