info:
  name: Notification Change State App (possible mutation)
  type: http
  seq: 2

http:
  method: GET
  url: "{{tree_base}}/comnity/Notification/ChangeStateApp?session={{session}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Potential state-changing endpoint from JS bundle string extraction.
