info:
  name: Auth Logout
  type: http
  seq: 5

http:
  method: GET
  url: "{{auth_base}}/oauth2/account/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Session mutation endpoint.
