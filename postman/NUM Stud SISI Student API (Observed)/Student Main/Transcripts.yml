info:
  name: Transcripts
  type: http
  seq: 7

http:
  method: POST
  url: "{{support2_base}}/stud/main/transcripts?sih={{sih}}&curID={{curId}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/transcript."
