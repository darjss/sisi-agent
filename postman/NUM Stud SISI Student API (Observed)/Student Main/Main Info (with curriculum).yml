info:
  name: Main Info (with curriculum)
  type: http
  seq: 2

http:
  method: POST
  url: "{{support2_base}}/stud/main/info?sih={{sih}}&curID={{curId}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed on transcript flow.
