info:
  name: Result Words
  type: http
  seq: 9

http:
  method: POST
  url: "{{support2_base}}/stud/main/resultwords"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/transcript."
