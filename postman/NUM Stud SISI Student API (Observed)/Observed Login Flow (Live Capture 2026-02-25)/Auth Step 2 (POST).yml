info:
  name: Auth Step 2 (POST)
  type: http
  seq: 4

http:
  method: POST
  url: "{{auth_base}}/oauth2/account/auth2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed auth second-step submit. Returns 302 during normal flow.
