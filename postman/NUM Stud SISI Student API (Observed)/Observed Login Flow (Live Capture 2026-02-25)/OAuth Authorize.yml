info:
  name: OAuth Authorize
  type: http
  seq: 7

http:
  method: GET
  url: "{{auth_base}}/oauth2/OAuth/Authorize?client_id={{clientId}}&redirect_uri={{redirectUri}}&state={{state}}&scope={{scope}}&response_type={{responseType}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed OAuth authorize redirect call.
