info:
  name: Auth Step 1 (GET)
  type: http
  seq: 1

http:
  method: GET
  url: "{{auth_base}}/oauth2/account/auth"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed first auth page load before credential submit.
