info:
  name: Auth Step 2 (GET)
  type: http
  seq: 3

http:
  method: GET
  url: "{{auth_base}}/oauth2/account/auth2?auth_type={{authType}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed second auth page.
