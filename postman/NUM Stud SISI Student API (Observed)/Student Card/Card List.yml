info:
  name: Card List
  type: http
  seq: 1

http:
  method: GET
  url: "{{support2_base}}/stud/card/list?sih={{sih}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/studentcard."
