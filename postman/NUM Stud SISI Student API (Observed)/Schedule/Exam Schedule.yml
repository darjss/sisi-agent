info:
  name: Exam Schedule
  type: http
  seq: 3

http:
  method: POST
  url: "{{support2_base}}/stud/Schedule/ExamSchedule?sih={{sih}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/examschedule."
