info:
  name: Get Components
  type: http
  seq: 1

http:
  method: GET
  url: "{{teach_sisi_base}}/stud/getComponents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Discovered in bundle. Likely read-only list of assessment components.
