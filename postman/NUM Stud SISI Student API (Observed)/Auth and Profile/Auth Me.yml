info:
  name: Auth Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{auth_base}}/resource/Me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed during app bootstrap.
