info:
  name: Student Photo AccessByMe
  type: http
  seq: 2

http:
  method: GET
  url: "{{phost_base}}/students/AccessByMe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed on home and studentcard contexts. Returns image/jpeg.
