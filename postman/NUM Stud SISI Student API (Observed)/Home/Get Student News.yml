info:
  name: Get Student News
  type: http
  seq: 2

http:
  method: POST
  url: "{{support2_base}}/stud/main/StudNews?sih={{sih}}&curid={{curId}}"
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observed on #/home."
