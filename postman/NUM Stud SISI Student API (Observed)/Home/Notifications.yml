info:
  name: Notifications
  type: http
  seq: 4

http:
  method: GET
  url: "{{tree_base}}/comnity/notification/GetReciveNotification?app=13"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Observed repeatedly across routes.
